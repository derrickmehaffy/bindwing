# Personal bindings — keep your real config out of the repo
Custom.4.2.binds

# Private VKB product-photo refs (not for redistribution)
ref/
# NOTE: images/ (EDRefCard device diagrams) is now TRACKED and shipped with attribution
# (MIT — see CREDITS.md). data/ JSON is regenerated from an EDRefCard clone via tools/extract.py.

# Logs, temp, editor/OS cruft
*.log
_check*
*.tmp
.DS_Store
Thumbs.db
.vscode/
.idea/
